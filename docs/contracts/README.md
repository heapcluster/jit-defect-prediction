# 三条契约

> 版本 1.0（**三份契约均已定稿/冻结**）｜ 创建 2026-09-16 ｜ 更新 2026-09-16 ｜ 负责人 蒋励 ｜ 审核 吕建江、刘帅华

## 这个目录是干什么的

三条线并行开工，靠的就是这三份契约。**契约不冻，两条线各自猜，最后一定返工。**

| 契约 | 文件 | 谁定 | 冻结时间 | 状态 | 主要影响谁 |
|---|---|---|---|---|---|
| 数据表字段 | `data-fields.md` | 蒋励 + 吕建江 | 第 2 周末（9/18） | **已冻结**（9/16 结清，版本 1.0） | 模型线 ↔ 后端线 |
| 特征列名 | `feature-columns.md` | 蒋励 | 第 2 周末（9/18） | **已冻结**（9/16 结清，版本 1.0） | 模型线 ↔ 后端线 |
| 接口格式 | `api-format.md` | 吕建江 | 第 3 周中（9/23） | **已定稿**，9/23 正式宣布冻结（9/16 结清，版本 1.0） | 后端线 ↔ 前端线 |

三份契约的末节原各有 3 项「待确认」，**已于 2026-09-16 全部结清**，结论写在各自文件的「冻结结论」一节。结论不再散落在聊天记录里。

## 契约管什么、不管什么

**管**：跨线共用的名字 —— 数据表字段名、特征列名、接口字段名与错误码。

**不管**：各线内部的实现细节。后端用什么 ORM、模型线用什么归一化方法、前端用什么组件库，都不写在这里。**契约写多了就变成设计文档，维护不动。**

判断标准一句话：**这个名字，是不是有两条以上的线会写到代码里用到？** 是，进契约；不是，放各线自己的文档。

## 变更流程

1. 想改契约，先在 Issue 里提出，写清「改什么、为什么、影响谁」。
2. 通知受影响的两条线（契约表里「主要影响谁」那一栏）。
3. 双方同步改代码，同一个 PR 里一起合入 —— **不允许只有一方改**。
4. 更新被改契约的变更日志与版本号，并同步更新本文档的变更日志。

**悄悄改契约是事故**：改的那一方代码跑得通，另一方第二天编译不过，还得花时间排查是谁改的。

## 冻结的含义

「冻结」不等于不能改，而是：**改要走上面的流程，并且必须通知到人。**

冻结日期是「对外宣布日」，不是「最后可以反悔的一天」—— 结论一经写入，改动即需走变更流程。数据表字段与特征列名定在 9/18、接口格式定在 9/23，是给协作者留的确认窗口。

## 协作者确认项

冻结结论由蒋励（组长、契约统稿）在 9/16 一次性裁定，其中三条涉及协作者的职责范围。**未在各自冻结点前提出异议，即按结论执行**：

| 项 | 结论所在 | 谁需确认 | 确认截止 |
|---|---|---|---|
| `prediction` 不存真实标签回填；详情页不展示「判对了没有」 | `data-fields.md`、`api-format.md` 冻结结论 | 刘帅华（页面侧） | 9/18 |
| 接口数量维持三个、不新增 `GET /api/models`；鉴权用固定令牌 `X-API-Key` | `api-format.md` 冻结结论 | 吕建江（后端侧） | 9/23 |
| `explanation` 定为 SHAP；不新增派生特征 | `api-format.md`、`feature-columns.md` 冻结结论 | 吕建江（后端侧）、全体 | 9/23 |

> 为何由组长先裁定：三条线的并行开工卡在契约上，等三方都点头才动笔，冻结点必然滑。先写死、后确认，异议走变更流程 —— 这条在 9/16 的启动会上已作为工作方式定下。

## 变更日志

| 日期 | 版本 | 变更人 | 主要变更 |
|---|---|---|---|
| 2026-09-16 | 0.9 | 蒋励 | 初版草案：三条契约的文件骨架与管理规则 |
| 2026-09-16 | 1.0 | 蒋励 | 三份契约定稿/冻结同步：状态表补「状态」列并标注实际进度；「待办」改为「协作者确认项」（三项结论、谁确认、截止日）；「冻结的含义」补「冻结日期是对外宣布日」的说明 |
