# data_model 线依赖清单
#
# 环境：Python 3.12.x；用哪种工具管理自选（conda / venv / uv 均可），见 docs/onboarding.md 第 2 节
# 安装：pip install -r data_model/requirements.txt
#
# 版本：**必须与 data_model/models/feature_order.txt 的 [dependencies] 段同源** —— 那份清单由
#       06_train_model.py 随模型一起产出，是 A 方案交付物；两处不一致，后端线就装不出能加载
#       .pkl 的环境。钉版本的目的是让别人的指标数字与你的可比 —— 版本不同，跑出来的数不一样。

pandas==3.0.5
numpy==2.5.3
scikit-learn==1.9.1
xgboost==3.4.1

# joblib：`.pkl` 的实际序列化格式（`joblib.dump`）—— **不是标准 pickle**：文件内含 numpy
# 缓冲（numpy_array_alignment_bytes），`pickle.load` 会报 invalid load key。它虽是
# scikit-learn 的传递依赖，但不受 pin 约束（跟着 sklearn 装什么版本就跑什么版本），
# 而后端线要按本清单装环境加载 `.pkl`，所以显式钉死 —— 与
# `data_model/models/feature_order.txt` 的 `[dependencies]` 段同源。
joblib==1.6.0

# SHAP：接口返回的 explanation 由它产出（契约三第三节）。**不能省** —— 少了它，
# 后端线按本清单装环境会缺依赖，预测接口的 explanation 字段直接起不来。
shap==0.52.0

GitPython

# 测试与代码风格：不参与指标计算，不钉版本
pytest
ruff

# SZZ 打标库：PySZZ 是否可用待实测（见 docs/data-pipeline.md 第 7 节）
# 若 Sprint 0 结束前仍跑不通，直接切自研简化版（label_method = 'szz_lite'），不引入该库
