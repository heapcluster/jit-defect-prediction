# SZZ 打标统计（02_szz_labeling.py 产出）

> 本文件可入库，是链路可复现与标签可信度的证据（见 `docs/data-pipeline.md` 第 3、6 节）

## 输入与判据

| 项 | 值 |
|---|---|
| 样本提交数（01 产出） | 11050 |
| 时间跨度 | 2005-12-12 17:53:59 ~ 2025-03-13 13:15:27 |
| 识别为修缺陷提交（判据 D4 双命中） | 2417 |

判据 D4 = 含缺陷编号 `AMQ-<数字>` **且** 含修复语义 `fix`/`bug`/`patch`（词首边界 `\b`，故 `dispatch`/`debug`/`prefix` 不算命中）。

### 判据自检（与 docs/data-pipeline.md 第 7 节登记的外沿对照）

| 判据 | 本脚本复算 | 文档登记 | 一致 |
|---|---|---|---|
| 只要求含 `AMQ-<数字>` 编号 | 6042 | 6042 | ✅ |
| 编号 **且** 含 `fix`/`bug`/`patch`（词首边界） | 2417 | 2417 | ✅ |
| 编号 **且** 含 `fix` 词形（词首边界） | 1939 | 1939 | ✅ |

自检结论：**全部一致，判据实现与文档口径相符**

## 各方法结果

| 指标 | szz | szz_lite |
|---|---|---|
| 打标成功数（进样本集） | 10893 | 10880 |
| **正样本数**（引入缺陷） | 4246 | 3018 |
| 负样本数 | 6647 | 7862 |
| **正样本比例** | **38.98%** | **27.74%** |
| 打标方法 `label_method` | `szz` | `szz_lite` |
| 回溯未命中的修复提交（不入样本，D5） | 258 | 259 |
| 　其中：引入方落在窗口/版本之外 | 0 | 1 |
| 　其中：根提交无父提交 | 0 | 0 |
| 　其中：diff 取数失败 | 0 | 0 |
| blame/日志调用次数 | 7454 | 7453 |
| 耗时（秒） | 1034.2 | 433.7 |

### 正样本比例是否落在合理区间（规格 szz-labeling）

- ✅ `szz` 正样本比例 38.98% 不在异常区间（判据见 `docs/data-pipeline.md` 第 6 节：落在 40%–60% 才需自查）
- ✅ `szz_lite` 正样本比例 27.74% 不在异常区间（判据同上）

## 两套方法对照（规格要求：差异必须可量化，不静默取其一）

| 项 | 值 |
|---|---|
| 两套都判为正样本 | 2100 |
| 仅 `szz` 判为正样本 | 2146 |
| 仅 `szz_lite` 判为正样本 | 918 |
| 标签不一致总数 | 3064 |
| 正样本分歧率 | 59.33%（不一致数 ÷ 两法正样本并集） |

`szz_lite` 多认出的前 10 条（粒度粗 ⇒ 容易把「只是同一个文件被改过」也算成引入方）：

| commit_hash |
|---|
| `00253b3219299d044a08fe7d846b4e0c3908ad65` |
| `0051c4ea6f579cab730d89364d0a53b043a90995` |
| `00921f22ff9a8792d7663ef8fadd4823402a6324` |
| `00cb9a566816357f3712e0de1ca0ee80815ddf0f` |
| `00daefc58e5e3573d7da14e3fd9b0354cc338bf6` |
| `00ee9491d9e21b85f44f23e05bc1bd6fd82dc2cc` |
| `01215e240dcfa71e868719900fac4f863bf72ba0` |
| `0146d5eb3e69e086ec19df1df1b2b7966c644d26` |
| `01b1f7f6945ba0897732b9eaae2ee1c9d50faf07` |
| `01b9e4bf5408476494f072cf7aebe7141ba6e340` |

## 说明

- 本产物是**带标签样本集，不含特征列**；特征见下一步 `03_*.py` 产出的 `commit_feature`
- `szz` 为自研标准行级 SZZ 实现：PySZZ 不在 PyPI（404），按兜底策略自研替代；契约一已于 1.2 版同步该口径（表二括注由「（PySZZ）」改为「标准行级 SZZ，自研实现」）
- `szz_lite` 为文件级回溯简化版：粒度粗、快一个量级，差异见上方对照表
- 回溯未命中的修复提交按 D5 排除出样本集，不当负样本；三个数字（总数 / 打标成功数 / 未判定数）在此表同时出示
