# 模型评估指标（06_train_model.py 产出）

> 本文件可入库，是 OpenSpec change `model-training-and-delivery` 第 3.3 条任务的证据。
> 两类指标的定义与公式见 `data_model/06_train_model.py` 文件头。

## 数据与参数

| 项 | 值 |
|---|---|
| 训练集 | `split_szz_train.csv` —— 7625 条 |
| 检验集 | `split_szz_test.csv` —— 3268 条 |
| 特征列 | 契约二第三节 14 项（顺序即交付清单顺序） |
| 标签 | `szz`（主标签；`is_bug_inducing`） |
| 训练集正样本比例 | 48.66% |
| 检验集正样本比例 | 16.40% |
| 随机种子 | `42`（显式固定，写进报告） |
| 判定阈值 | 0.5（服务端常量，非请求参数） |
| 检验集时间范围 | 2014-02-10 13:03:45 ~ 2025-03-13 13:15:27 |

> **注意训练/检验的正样本比例差**：训练集 48.66% vs 检验集 16.40%。
> 这是时间序切分的自然结果（早期历史里「一个修复提交指控多个引入方」更密集），
> **不是 bug**。但它意味着模型在检验集上会**系统性高估风险** —— 看 Precision 与
> Recall 的落差比看 Accuracy 有意义。

## 一、Effort-unaware 指标（检验集）

| 模型 | AUC | F1 | Precision | Recall | Accuracy |
|---|---|---|---|---|---|
| `lr_v1`（逻辑回归） | 0.6407 | 0.3265 | 0.2269 | 0.5821 | 0.6062 |
| `rf_v1`（随机森林） | 0.7463 | 0.4195 | 0.3046 | 0.6735 | 0.6943 |
| `xgb_v1`（XGBoost） | 0.7397 | 0.4133 | 0.2964 | 0.6828 | 0.6821 |

## 二、Effort-aware 指标（检验集）

沿某个排序依次累加，横轴 = 累计工作量占比、纵轴 = 累计召回率，取曲线下面积。
`P_norm` 以**随机排序（实测均值）= 0**、**真实密度上界 = 1** 两端线性归一：
`P_norm = (面积 − 随机基线) / (上界面积 − 随机基线)`。

**随机基线那一行是本节的自我校验** —— 它由 200 次随机排列取均值得到（固定种子），
若它偏得很远，说明面积算法有问题，其余数字一个都不能信。

> 锚点为什么用**实测均值**而不是写死的 `0.5`：在本数据集上，一次随机排列算出的
> 面积并不总在 0.5 —— 工作量是重尾的（LOC 中位数 23、最大 32 万行），单次结果被
> 「那几个超大提交排在第几位」主导。写死 0.5 会把归一化的基准带偏，实测均值则可核对
> —— 下表「±」后面那个数就是它的波动幅度，可以直接看出单次抽样有多不可靠。

### 工作量代理：LOC（改动行数 la+ld）

| 排序方式 | 模型 | 曲线下面积 | **P_norm** | Recall@20% | Recall@50% |
|---|---|---|---|---|---|
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `lr_v1` | 0.1950 | **-0.6665** | 0.0299 | 0.0634 |
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `rf_v1` | 0.3880 | **-0.2476** | 0.2034 | 0.3190 |
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `xgb_v1` | 0.3088 | **-0.4195** | 0.0746 | 0.1138 |
| 对照：按 `risk_score / 工作量` 降序 | `lr_v1` | 0.8677 | **0.7933** | 0.7276 | 0.9888 |
| 对照：按 `risk_score / 工作量` 降序 | `rf_v1` | 0.8741 | **0.8072** | 0.7463 | 0.9907 |
| 对照：按 `risk_score / 工作量` 降序 | `xgb_v1` | 0.8693 | **0.7967** | 0.7313 | 0.9907 |
| 上界：按**真实**缺陷密度降序 | — | 0.9629 | 1.0000 | 0.9832 | 1.0000 |
| 基线：随机排序（200 次抽样均值） | — | 0.5021 ± 0.0902 | 0.0000 | — | — |

### 工作量代理：改动文件数 nf

| 排序方式 | 模型 | 曲线下面积 | **P_norm** | Recall@20% | Recall@50% |
|---|---|---|---|---|---|
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `lr_v1` | 0.4742 | **-0.0523** | 0.1175 | 0.4608 |
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `rf_v1` | 0.6073 | **0.2415** | 0.2537 | 0.6884 |
| **模型排序**（`risk_score` 降序，**契约三当前口径**） | `xgb_v1` | 0.6123 | **0.2525** | 0.2332 | 0.6978 |
| 对照：按 `risk_score / 工作量` 降序 | `lr_v1` | 0.7381 | **0.5302** | 0.4049 | 0.8993 |
| 对照：按 `risk_score / 工作量` 降序 | `rf_v1` | 0.7602 | **0.5790** | 0.4496 | 0.9123 |
| 对照：按 `risk_score / 工作量` 降序 | `xgb_v1` | 0.7715 | **0.6040** | 0.5131 | 0.9067 |
| 上界：按**真实**缺陷密度降序 | — | 0.9509 | 1.0000 | 0.9832 | 1.0000 |
| 基线：随机排序（200 次抽样均值） | — | 0.4979 ± 0.0258 | 0.0000 | — | — |


### 这两张表说明什么

1. **按 `risk_score` 排序（契约三当前口径），在 LOC 工作量代理下不如随机排序** ——
   模型排序的 `P_norm` 为负，而随机基线在 0 附近。原因可解释：LOC 密度 `y/e` 的
   上界排序被**极小提交**主导（本数据集 LOC 中位数只有 23 行，一条改 1 行的缺陷
   修复密度就是 1.0，而一次改 19 万行的提交密度几乎为 0）；模型按**概率**排序，
   概率与提交大小正相关，于是把大提交排在前面 —— 而大提交恰恰是「按行数算不值当
   先审」的那一类。
2. **按 `risk_score / 工作量` 排序（Kamei 原文的 `R_d(x) = Y(x)/Effort(x)`）明显更好**：
   同一个模型、同一份特征，**只改排序方式**，`P_norm` 就回到正值以上。
   代价 —— **这需要契约变更**：契约三第三节规定风险列表按 `risk_score` 降序，改成按
   风险密度排序要同时改接口语义、前端排序展示与趋势看板的聚合口径。
   → 登记为契约变更**提案**，不在本 change 里静默改掉。
3. **工作量代理的选择会改变结论**：文件数代理下模型略好于随机，LOC 代理下不如随机。
   所以两种都报 —— 只报一种，等于替读者选了一个可能对他不利的口径。

> 上界那一行不是「另一个模型」的成绩，是上帝视角 —— 它只回答「这个指标的理论天花板
> 有多高」，不能拿去和模型比分数。

## 三、SHAP 单条解释耗时（tasks.md 4.4）

契约三第四节要求「预测接口 95% 的请求 < 500ms」。SHAP 是其中最主要的开销项，
故单独实测；**不达标只如实记录，不静默降级**（降级要提契约变更）。

> ⚠️ 本节是**实测耗时**，随机器负载波动 —— **连跑两次这一节会变，其余各表不变**。
> 所以核对「可复现」时要比对的是上面两张指标表，**不要把本节算进去**
> （把计时结果也要求逐字节一致，只会得出「不可复现」的错误结论）。

| 模型 | 解释器冷启动 | p50 | p95 | 最大 | 实测条数 | 是否满足 95% < 500ms |
|---|---|---|---|---|---|---|
| `lr` | 3.4 ms | 0.4 ms | **0.6 ms** | 1.6 ms | 100 | ✅ 满足 |
| `rf` | 65.3 ms | 113.6 ms | **127.7 ms** | 134.0 ms | 100 | ✅ 满足 |
| `xgb` | 71.0 ms | 3.9 ms | **5.0 ms** | 50.4 ms | 100 | ✅ 满足 |

> 冷启动是**一次性**开销（后端加载模型时构建解释器），不进请求路径；
> 请求路径上是 p95 那一列。

## 交付清单（A 方案）

`data_model/models/feature_order.txt`（**不入库**，随包交付）含：

- `.pkl` 文件路径与对应模型名
- 14 项特征顺序（契约二第三节的副本，不一致时以契约为准）
- 依赖版本 pin
- 风险判定阈值

同一份清单的内容打印在下面，便于在不 clone 模型目录的情况下核对：

```
# 模型交付清单（A 方案：后端加载 .pkl）
# 生成：data_model/06_train_model.py
#
# 说明：本清单是 `docs/contracts/feature-columns.md` 第三节的**副本**，
#       不是独立来源。两者不一致时**以契约为准**；契约二升版时本文件须同步。

[models]
lr_v1.pkl	逻辑回归
rf_v1.pkl	随机森林
xgb_v1.pkl	XGBoost

[feature_order]
 1. ns
 2. nd
 3. nf
 4. entropy
 5. la
 6. ld
 7. lt
 8. fix
 9. ndev
10. age
11. nuc
12. exp
13. rexp
14. sexp

[contract]
docs/contracts/feature-columns.md 第三节（14 项特征列，顺序即上表顺序）

[dependencies]
scikit-learn==1.9.1
xgboost==3.4.1
shap==0.52.0
pandas==3.0.5
numpy==2.5.3

[decision]
风险阈值 0.50（服务端常量，见 docs/contracts/api-format.md 第三节）
```
