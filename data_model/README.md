# data_model/ — 数据与模型线

**负责人**：蒋励

## 职责

拉取并固定 ActiveMQ 仓库版本 → SZZ 打标（PySZZ + 自研简化版对照）→ 抽取 Kamei 14 项特征 → 训练 2–3 个经典模型 → 评估。

## 交付物

带标记的样本集、特征表、模型文件、评估报告。

## 边界

- 数据集与模型文件不入库（见根目录 `.gitignore`）。
- 训练集/检验集必须**按提交时间**前 70% / 后 30% 切分，不许随机抽。
- 只在本目录内写代码。
