# backend 线依赖清单
#
# 环境：Python 3.12.x；用哪种工具管理自选（conda / venv / uv 均可），见 docs/onboarding.md 第 2 节
# 安装：pip install -r backend/requirements.txt
#
# 版本：首次跑通后回填 ==x.y.z（2026-09-17 本机实测跑通，Python 3.12 / SQLite 测试全绿）

fastapi==0.141.1
uvicorn==0.53.0
sqlalchemy==2.0.54
pymysql==1.2.3
python-dotenv==1.2.3

# 模型加载与解释（裁定 A+C；契约三冻结结论：explanation 用 SHAP）
scikit-learn==1.9.1
xgboost==3.4.1
shap==0.52.0
numpy==2.5.3
# 运行时加载路径：交付的 lr/rf .pkl 为 joblib 格式（Issue #46），不能只当 sklearn 传递依赖
joblib==1.6.0

# 测试（httpx 是 FastAPI TestClient 的依赖）
pytest==9.1.1
httpx==0.28.1
ruff==0.16.8
